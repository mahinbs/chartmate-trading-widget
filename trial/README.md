# Trial assets (legacy / reference)

The interactive **trial trading demo** now lives in the main app source tree:

- [`src/components/trial-demo/TrialMockDashboard.tsx`](../src/components/trial-demo/TrialMockDashboard.tsx) — routed from [`src/pages/TrialDashboardPage.tsx`](../src/pages/TrialDashboardPage.tsx) (`/1414ghgh`, `/#1414ghgh`).

`TradingSmartDashboard.html` in this folder is optional static reference only; it is not used by the Vite app.
